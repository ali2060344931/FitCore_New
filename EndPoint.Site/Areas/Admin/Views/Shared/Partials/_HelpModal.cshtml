<div class="modal fade help-modal" id="helpModal" tabindex="-1" aria-labelledby="helpTitle" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content help-modal-content">

            <div class="help-modal-glow help-modal-glow-1"></div>
            <div class="help-modal-glow help-modal-glow-2"></div>

            <div class="modal-header help-modal-header">
                <div class="help-modal-title-box">
                    <div class="help-modal-icon">
                        <i class="fa fa-question-circle"></i>
                    </div>

                    <div class="help-modal-heading">
                        <h5 id="helpTitle" class="modal-title help-modal-title">
                            راهنمای بخش
                        </h5>
                        <span class="help-modal-subtitle">
                            توضیحات، نکات مهم و راهنمای استفاده از این قسمت
                        </span>
                    </div>
                </div>

                <button type="button"
                        class="help-modal-close"
                        data-dismiss="modal"
                        aria-label="Close">
                    <i class="fa fa-times"></i>
                </button>
            </div>

            <div class="modal-body help-modal-body">
                <div class="help-content-wrapper">

                    <div class="help-content-side">
                        <div class="help-content-icon">
                            <i class="fa fa-lightbulb-o"></i>
                        </div>

                        <div class="help-side-badge">
                            راهنمای هوشمند
                        </div>
                    </div>

                    <div class="help-content-main">
                        <div class="help-content-label">
                            توضیحات این بخش
                        </div>

                        <div id="helpContent" class="help-content-text">
                        </div>
                    </div>

                </div>
            </div>

        </div>
    </div>
</div>

<style>
    /* =========================
       Help Modal - Premium UI
    ========================= */

    .help-modal .modal-dialog {
        max-width: 920px;
    }

    .help-modal .modal-content.help-modal-content {
        position: relative;
        border: 1px solid rgba(255, 255, 255, 0.06);
        border-radius: 28px;
        overflow: hidden;
        background: linear-gradient(180deg, rgba(255,255,255,0.98), rgba(248,250,252,0.96));
        box-shadow: 0 30px 80px rgba(2, 6, 23, 0.30), 0 10px 30px rgba(15, 23, 42, 0.14);
        backdrop-filter: blur(10px);
    }

    /* افکت‌های نوری */
    .help-modal-glow {
        position: absolute;
        border-radius: 50%;
        filter: blur(55px);
        opacity: 0.22;
        pointer-events: none;
        z-index: 0;
    }

    .help-modal-glow-1 {
        width: 220px;
        height: 220px;
        top: -70px;
        right: -40px;
        background: #f59e0b;
    }

    .help-modal-glow-2 {
        width: 260px;
        height: 260px;
        bottom: -120px;
        left: -80px;
        background: #3b82f6;
    }

    /* هدر */
    .help-modal-header {
        position: relative;
        z-index: 1;
        background: linear-gradient(135deg, #020617 0%, #111827 48%, #1f2937 100%);
        color: #ffffff;
        padding: 24px 28px;
        border-bottom: 1px solid rgba(255,255,255,0.06);
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .help-modal-title-box {
        display: flex;
        align-items: center;
        gap: 16px;
    }

    .help-modal-icon {
        position: relative;
        width: 58px;
        height: 58px;
        border-radius: 18px;
        background: linear-gradient(135deg, rgba(255,255,255,0.16), rgba(255,255,255,0.06));
        display: flex;
        align-items: center;
        justify-content: center;
        color: #facc15;
        font-size: 30px;
        box-shadow: inset 0 0 0 1px rgba(255,255,255,0.12), 0 12px 24px rgba(0,0,0,0.20);
    }

    .help-modal-heading {
        display: flex;
        flex-direction: column;
    }

    .help-modal-title {
        color: #ffffff;
        font-size: 22px;
        font-weight: 900;
        margin: 0;
        letter-spacing: -0.3px;
    }

    .help-modal-subtitle {
        display: block;
        margin-top: 6px;
        color: #cbd5e1;
        font-size: 13px;
        font-weight: 500;
    }

    .help-modal-close {
        width: 44px;
        height: 44px;
        border: none;
        border-radius: 14px;
        background: rgba(255, 255, 255, 0.10);
        color: #ffffff;
        font-size: 18px;
        cursor: pointer;
        transition: all 0.25s ease;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        box-shadow: inset 0 0 0 1px rgba(255,255,255,0.08);
    }

        .help-modal-close:hover {
            background: #ef4444;
            color: #ffffff;
            transform: rotate(90deg) scale(1.06);
        }

    /* بدنه */
    .help-modal-body {
        position: relative;
        z-index: 1;
        padding: 30px;
        background: radial-gradient(circle at top right, rgba(245, 158, 11, 0.10), transparent 24%), radial-gradient(circle at bottom left, rgba(59, 130, 246, 0.08), transparent 28%), linear-gradient(180deg, #f8fafc 0%, #f3f4f6 100%);
    }

    .help-content-wrapper {
        display: grid;
        grid-template-columns: 140px 1fr;
        gap: 22px;
        padding: 24px;
        border-radius: 24px;
        background: linear-gradient(180deg, rgba(255,255,255,0.96), rgba(255,255,255,0.90));
        border: 1px solid rgba(226, 232, 240, 0.95);
        box-shadow: 0 16px 40px rgba(15, 23, 42, 0.08), inset 0 1px 0 rgba(255,255,255,0.7);
    }

    /* ستون کناری */
    .help-content-side {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: flex-start;
        gap: 14px;
        padding-top: 6px;
    }

    .help-content-icon {
        width: 72px;
        height: 72px;
        border-radius: 22px;
        background: linear-gradient(135deg, #fff7ed, #fffbeb);
        color: #f59e0b;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 34px;
        box-shadow: 0 14px 30px rgba(245, 158, 11, 0.16), inset 0 0 0 1px rgba(245, 158, 11, 0.12);
    }

    .help-side-badge {
        padding: 7px 14px;
        border-radius: 999px;
        font-size: 12px;
        font-weight: 800;
        color: #92400e;
        background: linear-gradient(135deg, #fef3c7, #fde68a);
        box-shadow: inset 0 0 0 1px rgba(146, 64, 14, 0.08);
    }

    /* محتوای اصلی */
    .help-content-main {
        min-width: 0;
    }

    .help-content-label {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        margin-bottom: 14px;
        padding: 6px 12px;
        border-radius: 999px;
        background: #eef2ff;
        color: #4338ca;
        font-size: 12px;
        font-weight: 800;
        letter-spacing: 0.2px;
    }

    .help-content-text {
        color: #334155;
        font-size: 15px;
        line-height: 2.1;
        text-align: justify;
        width: 100%;
        word-break: break-word;
    }

        .help-content-text p {
            margin-bottom: 14px;
        }

        .help-content-text strong {
            color: #111827;
            font-weight: 800;
        }

        .help-content-text ul,
        .help-content-text ol {
            margin: 14px 0 14px 0;
            padding-right: 22px;
        }

        .help-content-text li {
            margin-bottom: 10px;
        }

        .help-content-text a {
            color: #2563eb;
            font-weight: 700;
            text-decoration: none;
        }

            .help-content-text a:hover {
                text-decoration: underline;
            }

        .help-content-text code {
            direction: ltr;
            display: inline-block;
            background: #0f172a;
            color: #f8fafc;
            padding: 2px 8px;
            border-radius: 8px;
            font-size: 13px;
        }

        .help-content-text::-webkit-scrollbar {
            width: 8px;
        }

        .help-content-text::-webkit-scrollbar-thumb {
            background: #cbd5e1;
            border-radius: 999px;
        }

    /* انیمیشن باز شدن */
    .help-modal.fade .modal-dialog {
        transform: scale(0.95) translateY(20px);
        opacity: 0;
        transition: all 0.28s ease;
    }

    .help-modal.fade.show .modal-dialog {
        transform: scale(1) translateY(0);
        opacity: 1;
    }

    /* بک‌دراپ خوشگل‌تر */
    .modal-backdrop.show {
        opacity: 0.6;
    }

    /* موبایل */
    @@media (max-width: 768px) {
        .help-modal .modal-dialog

    {
        max-width: calc(100% - 18px);
        margin: 10px auto;
    }

    .help-modal-header {
        padding: 18px 18px;
        align-items: flex-start;
        gap: 12px;
    }

    .help-modal-title-box {
        gap: 12px;
    }

    .help-modal-icon {
        width: 48px;
        height: 48px;
        border-radius: 15px;
        font-size: 24px;
    }

    .help-modal-title {
        font-size: 18px;
    }

    .help-modal-subtitle {
        font-size: 12px;
    }

    .help-modal-body {
        padding: 16px;
    }

    .help-content-wrapper {
        grid-template-columns: 1fr;
        gap: 18px;
        padding: 18px;
        border-radius: 20px;
    }

    .help-content-side {
        flex-direction: row;
        justify-content: flex-start;
        align-items: center;
    }

    .help-content-icon {
        width: 58px;
        height: 58px;
        border-radius: 18px;
        font-size: 28px;
    }

    .help-content-text {
        font-size: 14px;
        line-height: 2;
    }

    .help-modal-close {
        width: 40px;
        height: 40px;
        min-width: 40px;
    }

    }
</style>
